# 理科実験安全シミュレーター

中高理科の実験を安全に予習するためのブラウザゲームです。

## 起動方法

このゲームを実行するには、プロジェクトのディレクトリでローカルサーバーを起動します。

```bash
python3 -m http.server 8000
```

起動後、ブラウザで以下のURLにアクセスしてください。

<http://localhost:8000/>

サーバーを停止するには、起動したターミナルで `Ctrl+C` を押します。
